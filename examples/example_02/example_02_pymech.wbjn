import json

# start mechanical grpc server

#setup1 = system2.GetContainer(ComponentName="Setup")
#setup1.Edit()

model = system2.GetContainer(ComponentName="Model")
model.Edit()

#mech_port = setup1.StartGrpcServer()
mech_port = model.StartGrpcServer()
wb_script_result = json.dumps(mech_port)
