import json
sys = GetTemplate(TemplateName="EngData").CreateSystem()
comp = sys.GetComponent(Name="Engineering Data")
comp.Update(AllDependencies=True)
result = GetComponentState(Component=comp).State.ToString()
wb_script_result = json.dumps(result)
